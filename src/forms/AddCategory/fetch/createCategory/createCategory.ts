import server from "@src/modules/axios/instances";
import { FormData } from "../types";
import { apiIndex } from "../apis";

export default async function createCategory(data: FormData) {
	//TODO -- create the implementation
	
}
